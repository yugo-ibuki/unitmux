# huge-mouse

tmux で動いている `claude` や `codex` にテキストを送れるフローティングデスクトップアプリです。

[English](README.md)

## どんなアプリ？

huge-mouse は他のウィンドウの上に常駐し、tmux 内で動作中の AI コーディングアシスタント（`claude`、`codex`）にターミナルを切り替えずにコマンドを送信できます。

アクティブな AI ペインを自動検出し、ステータスを表示。AI が質問してきたときは、番号付き選択肢をクリックで応答できます。

## 必要なもの

- macOS または Linux
- [tmux](https://github.com/tmux/tmux) で `claude` または `codex` が動作していること

## インストール

[Releases](https://github.com/yugo-ibuki/huge-mouse/releases) ページから最新版をダウンロードし、アプリを開いてください。

## 使い方

1. tmux ペインで `claude` または `codex` を起動する
2. huge-mouse を開く — AI ペインが自動的に検出される
3. 上部のタグからペインを選択する
4. メッセージを入力して `Cmd+Enter` で送信

### ステータス表示

| 表示 | 意味 |
|------|------|
| 緑のドット | 入力受付中 |
| 黄色のドット | 応答待ち — 選択ボタンが表示される |
| グレーのドット | 処理中 |

### AI が質問してきたとき

`claude` や `codex` が番号付き選択肢（例：「1. Yes / 2. No」）を表示すると、ペインタグの横にクリック可能なボタンが現れます。クリックするだけで即座に応答できます。

### キーボードショートカット

| ショートカット | アクション |
|---------------|-----------|
| `Cmd+Enter` | 入力を送信 |
| `Cmd+↑` | 前のペインに切り替え |
| `Cmd+↓` | 次のペインに切り替え |

### 設定

歯車アイコンをクリックして**常時最前面表示**を切り替えられます。

## ライセンス

MIT
